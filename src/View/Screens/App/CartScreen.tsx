import React from 'react';
import ScreenShell from './ScreenShell';

const CartScreen = () => {
  return (
    <ScreenShell
      title="Cart"
      subtitle="Review your basket and apply coupons before checkout."
    />
  );
};

export default CartScreen;
