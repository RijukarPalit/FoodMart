import React from 'react';
import ScreenShell from './ScreenShell';

const ExploreScreen = () => {
  return (
    <ScreenShell
      title="Explore"
      subtitle="Browse categories, search for brands, and discover weekly deals."
    />
  );
};

export default ExploreScreen;
