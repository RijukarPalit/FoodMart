import React from 'react';
import ScreenShell from './ScreenShell';

const OrdersScreen = () => {
  return (
    <ScreenShell
      title="Orders"
      subtitle="Track deliveries, reorder favorites, and view past receipts."
    />
  );
};

export default OrdersScreen;
