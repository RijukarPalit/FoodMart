import React from 'react';
import ScreenShell from './ScreenShell';

const HomeScreen = () => {
  return (
    <ScreenShell
      title="Fresh Finds"
      subtitle="Start with curated picks and seasonal discounts for your pantry."
    />
  );
};

export default HomeScreen;
